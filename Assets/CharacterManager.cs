using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CharacterManager : MonoBehaviour {

    public ArmorClassManager ACManager;
    public InitiativeManager InitManager;
    public MoneyManager MoneyManager;
    public PassiveWisdomManager PWManager;
    public ProficiencyManager ProfManager;
    public SavingThrowManager STManager;
    public SkillsManager SkillManager;
    public SpeedManager speedManager;
    public StatManager StatManager;

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
